<nav>
    <ul>
        <li><a href="{{ path('sortie_list') }}" style="text-decoration: none">Home</a></li>
        {% if (app.user) %}
            <li><a href="{{ path('app_logout') }}" style="text-decoration: none">Logout ({{ app.user.userIdentifier }})</a></li>
        {% else %}
            <li><a href="{{ path('app_login') }}" style="text-decoration: none">Login</a></li>
            <li><a href="{{ path('app_register') }}" style="text-decoration: none">Register</a></li>
        {% endif %}
    </ul>
</nav>