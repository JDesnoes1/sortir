<nav>
    <ul class="nav-perso">
            <li class="sortir">
                {% if (app.user) %}
                    <a class="txt-blanc" href="{{ path('sortie_list') }}"><strong>SortirENI</strong></a>
                {% else %}
                    <a><strong>SortirENI</strong></a>
                {% endif %}
            </li>
        {% if (app.user) %}
            <li class="nav-item dropdown">
                <a class="txt-blanc dropdown-toggle" href="#" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">Mon profil</a>
                <ul class="dropdown-menu">
                    <li><a href="{{ path('participant_show', {'id': app.user.id}) }}">Voir mon profil</a></li>
                    <li><a href="{{ path('participant_sorties', {'id': app.user.id}) }}">Mes Sorties</a></li>
                </ul>
            </li>
            <li class="nav-item"><a class="txt-blanc" href="{{ path('sortie_add') }}">Ajouter une sortie</a></li>
            {% if is_granted('ROLE_ADMIN') %}
                <li class="nav-item dropdown">
                    <a class="txt-blanc dropdown-toggle" href="#" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">Participants</a>
                    <ul class="dropdown-menu">
                        <li><a href="{{ path('app_register') }}">Enregistrer un nouveau participant</a></li>
                        <li><a href="{{ path('participant_list') }}">Liste des participants</a></li>
                    </ul>
                </li>
                <li class="nav-item dropdown">
                    <a class="txt-blanc dropdown-toggle" href="#" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">Villes</a>
                    <ul class="dropdown-menu">
                        <li><a href="{{ path('ville_add') }}">Ajouter une ville</a></li>
                        <li><a href="{{ path('ville_list') }}">Liste des villes</a></li>
                    </ul>
                </li>
                <li class="nav-item dropdown">
                    <a class="txt-blanc dropdown-toggle" href="#" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">Campus</a>
                    <ul class="dropdown-menu">
                        <li><a href="{{ path('campus_add') }}">Ajouter un campus</a></li>
                        <li><a href="{{ path('campus_list') }}">Liste des campus</a></li>
                    </ul>
                </li>
                {% endif %}
                <li class="imgProril">
                    {% if app.user.photo %}
                        <img class="compte" style="height: 40px; width: 40px; border-radius: 50%" src="{{ asset('img/'~app.user.photo) }}" alt="{{ app.user.nom }}">
                    {% endif %}
                </li>
                <li class="nav-item">
                    <a class="txt-blanc" href="{{ path('app_logout') }}">({{ app.user.userIdentifier }}) Se déconnecter</a>
                </li>
            {% else %}
                <li class="nav-item">
                    <a class="txt-blanc" href="{{ path('app_login') }}">Se connecter</a>
                </li>
            {% endif %}
    </ul>
</nav>
