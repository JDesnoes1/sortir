{% extends 'base.html.twig' %}

{% block title %}
    {{ parent() }} | Ajouter une ville
{% endblock %}

{% block body %}
    <div>
        {{ form_start(villeForm)}}
        {{ form_widget(villeForm)}}
        <a class="btn btn-outline-primary">{% block buttonLabel %}Ajouter une ville{% endblock %}</a>
        {{ form_end(villeForm)}}
    </div>
{% endblock %}

